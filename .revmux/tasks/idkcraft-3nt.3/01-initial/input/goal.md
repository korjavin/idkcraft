- Bead: perception computes nearest hostile (creepers excluded from fight candidates), `hostile_distance` (number or null) and `hostile_near_player` (≤ 6 blocks from the player); both fold into `stateKey` so a hostile approaching forces a fresh brain call. `behaviours/fight.js`: GoalFollow(mob, 2) once per target, look + `bot.attack` once per tick when ≤ 3 blocks, equip a sword if present, stop once when the target dies/despawns. Wired as `BEHAVIOURS.fight`. The temporary brain.js guard ("fight → stub when no hostile_distance") is removed because perception now always sends the field.
- Correct only if:
  - `hostile_distance` is present in every state (null when no hostile), so the stub policy and the removed guard are consistent — no crash on `.toFixed` of null in brain.js stateToText
  - switching follow → fight → follow does not leave a stale pathfinder goal: ctx.lastGoalKey handling in fight.js and follow.js must not confuse each other (e.g. fight sets key `fight:<id>`, follow later sees a different key and re-issues — verify the reverse too)
  - a dead or despawned target (`entity.isValid === false`, or entity gone from bot.entities) stops the goal once and does not swing at a ghost
  - `bot.attack` is called at most once per tick and only within 3 blocks; `lookAt` failure (rejected promise) cannot crash the tick — check every promise from mineflayer here is caught
  - sword equip is best-effort: a rejected `bot.equip` must not throw out of the tick loop
  - stateKey rounding of hostile_distance does not thrash the brain call every tick while a mob stands still (bounded granularity), and does not hide a mob crossing the 8-block / 3-block thresholds
  - the 14 tests would fail if the swing gate, the creeper exclusion, or the dead-target stop were inverted or removed
- This is the merge gate: answer whether anything here should not ship. Finding nothing is a valid answer.
