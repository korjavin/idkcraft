- Bead: local, brain-free scout. Every 5 s, `bot.findBlocks` within 16 blocks for valuable ore (diamond, emerald, ancient_debris, gold, iron, lapis, redstone + deepslate variants; coal/copper excluded), dedup by block position (Set capped at 5000), chat `<ore> x<n> at x y z` for new veins only, max 3 lines per scan. Hooked at the every-tick seam in index.js; never touches the pathfinder; must not chat when no player is online (idle branch returns before the seam).
- Correct only if:
  - no chat and no findBlocks when the idle branch runs (no player online) — check the seam placement against the idle return in the tick
  - a vein already reported is never reported again while the bot runs (dedup), and the 5000 cap cannot make an old vein re-report every scan
  - findBlocks is not called every tick (cadence 5 s), and the scan cost is bounded (count limit passed to findBlocks)
  - chat lines are capped at 3 per scan even when 50 new blocks appear
  - `bot.registry` may be undefined until spawn: the lazy creation must not throw before spawn and must create once
  - the exported `findNearestBlock` (for the upcoming `find me <block>` bead) resolves plain names to ore ids sanely (e.g. 'coal' → coal_ore and deepslate_coal_ore)
  - the tests would fail if dedup, the cap, the cadence or the 3-line limit were removed
- This is the merge gate: answer whether anything here should not ship. Finding nothing is a valid answer.
